/** Placeholder settings route. Governance/policy UI lands in later tasks. */
export function Settings() {
  return (
    <section className="space-y-3">
      <h1 className="text-2xl font-semibold">Settings</h1>
      <p className="text-muted-foreground">
        Placeholder route. Governance policy and account settings will be built
        in later tasks.
      </p>
    </section>
  );
}
