# Base URL of the ai-usage-backend REST API this dashboard consumes.
# The dashboard is a pure API consumer — it performs no server-side rendering
# and persists no data of its own. All Vite env vars must be prefixed VITE_.
VITE_BACKEND_URL=http://localhost:8080
